  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js" >
  </script>
<h1>sign up!</h1>

<ul>
<% flash[:errors].to_a.each do |error| %>
  <li> <%=error%> </li>
<% end %>
</ul>

<form id="new_user" action="<%=users_url%>" enctype="multipart/form-data" method="post"  accept-charset="UTF-8">
  <input name="utf8" type="hidden" value="✓">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <label for="email">email
  <input type="text" name="user[email]" value="<%=@user.email%>">
  </label>
  <br>
  <label for="username">username
  <input type="text" name="user[username]" value="<%=@user.username%>">
  </label>
  <br>
  <label for="password">password</label>
  <input type="password" name="user[password]">
  </label>
  <br>
  <button>Submit</button>
  <br>
  <br> Tell us more! <br>
  <br>
  <label for="fname">First Name</label>
  <input type="text" name="[user]fname" value="<%=@user.fname%>">
  <br>
  <label for="lname">Last Name</label>
  <input type="text" name="[user]lname" value="<%=@user.lname%>">
  <br>

  <label for="user[avatar]">Profile Image</label>
  <input type="file" name="user[avatar]" id="user_avatar">
  <br>
  <img id="avatar" src="" alt="" width=100/>
  <br>
  <button>Submit</button>

</form>

  <script type="text/javascript">
  var readURL = function(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function(e) {
          $("#avatar").attr('src', e.target.result);
        }
        reader.readAsDataURL(input.files[0]);
      }
    }

    $("input[type=file]").change(function(){
        readURL(this);
    });
  </script>

  <div>
  or  <a id="goog-signin" href="/auth/google_oauth2">sign in with Google</a>
  </div>
